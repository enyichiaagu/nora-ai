import React from 'react';

const Hero: React.FC = () => {
  return (
    <section className="h-[90vh] bg-app-primary noice">
      <h1>Hero Component</h1>
    </section>
  );
};

export default Hero;